export class Snake {

    // public size: number = 3;
    // public direction: string = 'right';
    // public body: CellState[] = [];
    // public isDead: boolean = false;
    // public head!: CellState;

    constructor() {
        // this.initBody();
    }

/*
    initBody() {

        // let initialX = 5;
        // let initialY = 5;

        // for (let i = 0; i < this.size; i++)
        //     this.body.push(new CellState(initialX - i, initialY));

        // this.head = this.body[this.size - 1];
    }
*/

}
